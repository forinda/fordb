# Keyboard & Palette

The command palette (`Ctrl/Cmd-K`) reaches every action; the SQL editor runs on `Mod-Enter`. fordb is built to be driven without leaving the keyboard.

_Full shortcut reference coming soon._

# Connections

Postgres and SQLite (local file, remote libsql/Turso, and embedded replicas); MongoDB in v0.3. Credentials are stored in the OS keychain, never in the profiles file or the renderer.

_Full walkthrough coming soon._

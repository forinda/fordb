# Schema & Structure

Browse the schema tree, view a table's columns/keys/indexes and a reconstructed `CREATE TABLE`, and use the create-table / create-database designers. Every DDL change is previewed as SQL and confirmed before it runs.

![fordb create-table designer](/screenshots/designer.png)

_Full walkthrough coming soon._
